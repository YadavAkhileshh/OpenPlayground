/**
 * GameStateManager.js
 * Helpers for state transitions.
 */

import { GameStates } from '../core/Settings.js';

export class GameStateManager {
    // Currently logic is simple enough to reside in Game.js
    // keeping this as a place holder for complex state logic (menus, transitions)
}
